{
  "private": true,
  "name": "phonic-nonsense",
  "version": "2.0.0",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "deploy": "npm run build && echo phonic-nonsense.euans.space > build/CNAME && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "preact-cli": "^3.0.0",
    "prettier": "^2.5.1",
    "sirv-cli": "^1.0.3",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
